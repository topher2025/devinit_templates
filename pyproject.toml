[project]
name = "devinit-templates"
version = "0.1.0"
description = "devinit templates"
readme = "README.md"
authors = [
    { name = "Christopher Lewis", email = "topher.the.good.ninja@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []


[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
